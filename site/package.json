{
	"name": "@vicugna/site",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"audit": "dotenv -e .env -- sh -c 'lighthouse \"$DEV_URL\" --output=html --output-path=./lighthouse-report.html --chrome-flags=\"--headless\" --view'",
		"dev": "astro dev",
		"build": "astro build",
		"start": "npm run build && astro preview",
		"preview": "astro preview"
	},
	"dependencies": {
		"@tailwindcss/vite": "^4.3.3",
		"astro": "^5.0.0",
		"tailwindcss": "^4.3.3"
	},
	"devDependencies": {
		"dotenv-cli": "^11.0.0",
		"lighthouse": "^12.8.2",
		"wrangler": "^4.47.0"
	}
}
