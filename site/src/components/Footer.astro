---
import { commonContent } from "../lib/content"
import { routes, type Language } from "../lib/routes"

type Props = { language: Language }

const { language } = Astro.props
const content = commonContent[language]
---

<footer class="border-t border-earth/20 py-10 sm:py-14">
	<div class="grid gap-8 md:grid-cols-[1fr_auto] md:items-end">
		<div>
			<p class="font-serif text-2xl sm:text-3xl">{content.footerTitle}</p>
			<p class="mt-3 max-w-xl text-sm text-earth/65">{content.footerText}</p>
			<p class="mt-5 max-w-2xl text-xs leading-5 text-earth/50">{content.footerRights}</p>
		</div>
		<a href={routes.contact[language]} class="text-xs font-bold uppercase tracking-[0.18em] underline decoration-clay underline-offset-8">
			{content.navigation.contact}
		</a>
	</div>
</footer>
