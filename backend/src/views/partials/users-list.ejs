<%
	const regularUsers = users.filter((user) => user.role === "user")
	const adminUsers = users.filter((user) => user.role === "admin")
%>

<section id="users-page" <% if (typeof useOobSwap !== "undefined" && useOobSwap) { %>hx-swap-oob="outerHTML"<% } %> class="card mt-6 bg-base-100 shadow">
	<div class="border-b border-base-300 px-5 pt-5">
		<h2 class="text-lg font-semibold text-slate-900">Usuarios registrados</h2>

		<nav class="tabs tabs-border mt-5" aria-label="Tipos de usuario">
			<a href="#users"
				class="tab"
				:class="activeUsersTab === 'users' ? 'tab-active' : ''"
				@click="activeUsersTab = 'users'">
				Encargados
			</a>
			<a href="#admins"
				class="tab"
				:class="activeUsersTab === 'admins' ? 'tab-active' : ''"
				@click="activeUsersTab = 'admins'">
				Admins
			</a>
		</nav>
	</div>

	<div x-show="activeUsersTab === 'users'">
		<%- include("./users-list-panel", { users: regularUsers, emptyMessage: "No hay encargados creados." }) %>
	</div>

	<div x-show="activeUsersTab === 'admins'">
		<%- include("./users-list-panel", { users: adminUsers, emptyMessage: "No hay admins creados." }) %>
	</div>
</section>
